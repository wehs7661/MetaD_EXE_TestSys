lambda: READ FILE=COLVAR VALUES=lambda IGNORE_TIME IGNORE_FORCES

METAD ...
ARG=lambda 
SIGMA=0.0001     # small SIGMA ensure that the Gaussian approaximate a delta function
HEIGHT=1.2388545199729883     # 0.5 kT
PACE=100000000        # should be nstexpanded
GRID_MIN=0     # index of alchemical states starts from 0
GRID_MAX=8     # we have 6 states in total
GRID_BIN=8
TEMP=298
BIASFACTOR=50
LABEL=metad    # it's not clear how GRID parameters will have influences here
RESTART=YES
... METAD

rw: REWEIGHT_BIAS TEMP=298
hh: HISTOGRAM ARG=lambda GRID_MIN=0 GRID_MAX=8 GRID_BIN=8 BANDWIDTH=0.02 LOGWEIGHTS=rw CLEAR=5000 NORMALIZATION=true
DUMPGRID GRID=hh FILE=histogram/hist.dat STRIDE=5000
